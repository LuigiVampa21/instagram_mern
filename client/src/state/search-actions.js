import axios from "axios";
import { headersConfig } from "utils/headers";
import { setSearchArray } from './index';

export const getUserList = value => {
    return async dispatch => {
        try{

        }catch(err){
            
        }
    }
}